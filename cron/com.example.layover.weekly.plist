<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN"
  "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  macOS launchd agent for the Layover weekly run.
  Install:
    cp cron/com.example.layover.weekly.plist ~/Library/LaunchAgents/
    # edit the path below to your checkout, then:
    launchctl load ~/Library/LaunchAgents/com.example.layover.weekly.plist
  Runs Mondays at 07:00; StartCalendarInterval fires on next wake if asleep.
-->
<plist version="1.0">
<dict>
  <key>Label</key>
  <string>com.example.layover.weekly</string>

  <key>ProgramArguments</key>
  <array>
    <string>/opt/layover/cron/layover-weekly.sh</string>
  </array>

  <key>StartCalendarInterval</key>
  <dict>
    <key>Weekday</key><integer>1</integer>
    <key>Hour</key><integer>7</integer>
    <key>Minute</key><integer>0</integer>
  </dict>

  <!-- AirTrail dedup config; or place an airtrail.ini next to the script. -->
  <key>EnvironmentVariables</key>
  <dict>
    <key>LAYOVER_DIR</key><string>/opt/layover</string>
    <!-- <key>AIRTRAIL_URL</key><string>http://airtrail-host:3006</string> -->
  </dict>

  <key>StandardOutPath</key>
  <string>/opt/layover/flight-mail-out/launchd.out.log</string>
  <key>StandardErrorPath</key>
  <string>/opt/layover/flight-mail-out/launchd.err.log</string>
</dict>
</plist>
