# Copy to airtrail.ini (git-ignored, chmod 600) and fill in.
# Used by airtrail.py / populate.py to dedup candidates against existing flights
# via GET /api/flight/list. The api_key is a SECRET — never commit the real one.
[airtrail]
url = http://vmgpu:3006
api_key = REPLACE_ME
